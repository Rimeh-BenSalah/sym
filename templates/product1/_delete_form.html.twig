<form method="post" action="{{ path('app_product1_delete', {'id': product1.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product1.id) }}">
    <button class="btn">Delete</button>
</form>
